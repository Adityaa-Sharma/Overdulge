name: Developer Agent
on:
  issues:
    types: [labeled]
  pull_request_review:
    types: [submitted]
concurrency:
  # Scope per triggering issue/PR, NOT one global group. A single global group
  # only keeps one running + one pending run; every extra label event is
  # cancelled and lost, so items labelled together silently never get built.
  # Per-item grouping lets distinct issues run in parallel. Working the same
  # item twice is still prevented by the claim protocol (in-progress + assignee)
  # in dev.md, which is the real lock.
  group: dev-agent-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: false
permissions:
  contents: write
  issues: write
  pull-requests: write
jobs:
  develop:
    if: >
      (github.event_name == 'issues' &&
        (github.event.label.name == 'ready-for-dev' ||
         github.event.label.name == 'ready-for-fix')) ||
      (github.event_name == 'pull_request_review' &&
        github.event.review.state == 'changes_requested')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/create-github-app-token@v1
        id: app
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
      - uses: actions/checkout@v4
        with:
          token: ${{ steps.app.outputs.token }}
          fetch-depth: 0
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ steps.app.outputs.token }}
          allowed_bots: "overdulge"
          model: "claude-opus-4-8"
          prompt: |
            Read agents/dev.md and execute one work item. Trigger context:
            event=${{ github.event_name }},
            issue=${{ github.event.issue.number || 'n/a' }},
            pr=${{ github.event.pull_request.number || 'n/a' }}.
            Follow the claim protocol strictly; if the item is already claimed
            or labeled escalation:human/blocked, exit without changes.
          claude_args: "--max-turns 100 --allowedTools Bash,Read,Write,Edit,Glob,Grep,WebSearch,WebFetch"
        env:
          GH_TOKEN: ${{ steps.app.outputs.token }}
